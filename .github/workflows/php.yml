name: 'php'
on: push

jobs:
  php:
    runs-on: ${{ matrix.operating-system }}
    strategy:
      fail-fast: false
      matrix:
        operating-system: [ macos-latest ]
    steps:
      - name: "List .NET SDK"
        run: dotnet --list-sdks
      - name: "Search .NET SDK"
        run: ls -al /usr/local/share/dotnet/sdk
